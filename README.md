# bulk-excel-processor
1) npm install
2) npm run setup - will setup the Postgres connection 
3) npm run generate - create the excel file ( will take time since it will be big )
4) npm run start - insert data from excel to db

ENV needs to 
